# Class representing a set of intervals

Class representing a set of intervals

## Usage

``` r
# S3 method for class 'breaks'
format(x, ...)

# S3 method for class 'breaks'
print(x, ...)

is.breaks(x, ...)
```

## Arguments

- x:

  A breaks object

- ...:

  Unused
